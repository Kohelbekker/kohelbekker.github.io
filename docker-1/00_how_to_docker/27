docker service logs $(docker service ps engineering-bay -f "name=engineering-bay.1" -q) -f

# Altern: docker service logs -f engineering-bay
# docker service ps engineering-bay
# -f --follow command will continue streaming the new output from the service’s STDOUT and STDERR
