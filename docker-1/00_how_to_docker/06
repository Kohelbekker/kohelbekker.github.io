docker run -p 5000:80 --name overlord --restart=always -d nginx
# -d -detach mode, run container on background
# -p ports mapping
# restart: https://docs.docker.com/config/containers/start-containers-automatically/

